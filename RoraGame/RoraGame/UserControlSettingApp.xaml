<UserControl x:Class="RoraGame.UserControlSettingApp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RoraGame"
             mc:Ignorable="d" 
             
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            TextElement.FontWeight="Regular"
            TextElement.FontSize="13"
            TextOptions.TextFormattingMode="Ideal" 
            TextOptions.TextRenderingMode="Auto"        
            FontFamily="{DynamicResource MaterialDesignFont}"
            Background="{DynamicResource MaterialDesignPaper}">
    <Grid VerticalAlignment="Top" HorizontalAlignment="Left" Margin="20">
        <DockPanel>
            <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="Steam" Text="C:\Program Files (x86)\Steam" Margin="0 0 0 0" MinWidth="200"/>
            <Button Content="{materialDesign:PackIcon SettingsHelper}" ToolTip="Đường dẫn" Height="25" Margin="15 0 0 0" VerticalAlignment="Bottom"/>
        </DockPanel>
    </Grid>
</UserControl>
