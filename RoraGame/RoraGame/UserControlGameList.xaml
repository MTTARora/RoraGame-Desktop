<UserControl x:Class="RoraGame.UserControlGameList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RoraGame"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" >
    <ListView Name ="LsGames" Margin="10 0 0 0">
        <ListView.View>
            <GridView>
                    <GridViewColumn
                        DisplayMemberBinding="{Binding No}"
                        Header="#" />
                    <GridViewColumn
                        DisplayMemberBinding="{Binding Name}"
                        Header="Tên" />
                    <GridViewColumn
                        DisplayMemberBinding="{Binding Platform}"
                        Header="Nền tảng" />
                <GridViewColumn
                        Header="Thuê Game ngay">
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <Button Tag="{Binding Thue}" Content="Thuê Game"/>
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn
                        DisplayMemberBinding="{Binding Description}"
                        Header="Thông tin" />
            </GridView>
            </ListView.View>
        </ListView>
</UserControl>
